# LONMA Night Maintenance Implementation Plan

1. Record the clean baseline and public route inventory.
2. Add failing tests for the six-case asset manifest and required asset paths.
3. Add the manifest, folder guide, and non-destructive image preparation tool.
4. Add failing tests for canonical, Open Graph, Twitter, sitemap, and robots
   coverage across every public route.
5. Add shared SEO rendering helpers, update generated pages, and patch the
   remaining static pages without changing body markup.
6. Add failing internal-link and local-asset audits, then fix only confirmed
   defects.
7. Run the full automated suite and regenerate checked-in pages.
8. Start a local server and verify desktop, tablet, and mobile layouts plus
   browser console output.
9. Write the morning report with completed work, missing case media, test
   totals, and deferred external actions.
10. Commit only the night-maintenance scope, push the branch, and create a
    Draft PR without merging or deploying.
